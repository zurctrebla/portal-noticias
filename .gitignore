/uploads
/cache
/upgrade
/ai1wm-backups
/w3tc-config
/wflogs
/smush-webp
/index.php
/reports

# Traduções pt_BR: versionadas p/ entrarem na imagem Docker (senão date_i18n cai p/ inglês).
# Mantém runtime (.mo/.l10n.php/.json), ignora fonte .po (não usada em runtime, ~3MB).
languages/**/*.po

# Node.js
themes/bahia_refactor/node_modules
themes/bahia_refactor/dist
*.log

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Tema padrão do WordPress 6.8, escrito pelo container ao subir o core.
# Não é usado (o site roda Newspaper) e não deve entrar na imagem.
themes/twentytwentyfive/
